export type MousePosition = {
  x: number;
  y: number;
};

export type TiltState = {
  x: number;
  y: number;
};

export type HolographicConfig = {
  hue: number;
  saturation: number;
  brightness: number;
  rotation: number;
  intensity: number;
};

export type HolographicStyles = {
  backgroundImage: string;
  backgroundSize: string;
  opacity: number;
};
